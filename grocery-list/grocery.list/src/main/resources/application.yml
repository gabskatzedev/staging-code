server:
  port: 8080

spring:  
  datasource:    
    url: jdbc:postgresql://pega2020usf.c4rex4fxmjgm.us-east-2.rds.amazonaws.com/postgres
    username: gabskatzedev
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:      
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true